= form_for @core, method: :put, url: wizard_path do |f|
  - if f.object.errors.any?
    .error_messages
      - f.object.errors.full_messages.each do |error|
        %p= error
  %fieldset
    %legend Core Identity
    %div
    %div
      = f.submit 'Next Step'
