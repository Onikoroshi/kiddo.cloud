= render "account/steps/step_navigation"
    
.col-xs-12
  - if @account.drop_ins.any?
    .alert.alert-warning
      .alert-icon
        %i.material-icons warning
      %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
        %span{"aria-hidden" => "true"}
          %i.material-icons clear
      You previously chose to register as a drop-in. If you choose a monthly plan instead, your drop-in selections will be reset.
      
  - elsif @account.children_enrolled?(@center.current_program)
    .alert.alert-warning
      .alert-icon
        %i.material-icons warning
      %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
        %span{"aria-hidden" => "true"}
          %i.material-icons clear
      You previously chose a monthly plan. If you choose to register for a drop-in plan instead, your monthly selections will be reset.
      
  %h3.text-center
    Select a Monthly Plan or One-time Dropin/Camp Day
  
.col-md-8.col-md-offset-2.col-xs-10.col-xs-offset-1
  #attendance-selection-chooser
    .col-xs-6.text-center
      = link_to "Monthly", edit_account_attendance_selection_path(@account), class: "btn btn-lg btn-block btn-info"
    .col-xs-6.text-center
      = link_to "Drop In", new_account_drop_ins_path(@account), class: "btn btn-lg btn-block btn-danger"
