.col-md-8.col-md-offset-2.col-xs-10.col-xs-offset-1.text-center
  %h2 Program Payment
  = image_tag ("credit-cards.png")

  %br.cleaner
  
  %h5
    Your total: 
    %strong 
      #{PlanSummarizer.new(@account).total!}
  
  %br.cleaner
  
  = form_tag account_subscription_path, id: "payment-form" do |form|
    .form-group.label-floating
      %label{:for => "card-element", class: "control-label"}
        Credit or Debit Card
      #card-element.form-control
        / a Stripe Element will be inserted here.
      / Used to display Element errors
      #card-errors{:role => "alert"}
    = submit_tag "Pay & Finish Registration", class: "btn btn-info pull-right"
